name: Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Run build
        run: |
          # Example: simulate build
          echo "Build success"
          # exit 1  # uncomment to test failure

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v3

      - name: Deploy
        run: |
          echo "Simulating deploy..."
          # exit 1  # uncomment to test failure

  notify:
    runs-on: ubuntu-latest
    needs: build            # depends only on build
    if: always()           # always run, even if build fails
    steps:
      - name: Send Discord notification
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          # Default to success
          STATUS="success"

          # Check if build failed
          if [[ "${{ needs.build.result }}" != "success" ]]; then
            STATUS="failure"
          fi

          # Prepare message
          if [ "$STATUS" == "success" ]; then
            MESSAGE="✅ Build succeeded on branch $GITHUB_REF!"
          else
            MESSAGE="❌ Build FAILED on branch $GITHUB_REF!"
          fi

          # Send Discord message
          curl -H "Content-Type: application/json" \
               -X POST \
               -d "{\"content\":\"$MESSAGE\"}" \
               $DISCORD_WEBHOOK_URL